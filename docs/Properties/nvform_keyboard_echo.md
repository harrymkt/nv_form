# nvform_keyboard_echo
The echo flag, global.

`int nvform_keyboard_echo = TEXTFLAG_ENTRY_CHARACTERS;`

## Remarks:
If `rely_on_nvform_echo` property in `nv_form` class is `true` (which default), this property will always be used. Otherwise, this property will be used at every new form.
