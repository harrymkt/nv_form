# old_pos
Where the cursor was before it moved.

`int old_pos;`

## Remarks:
-1 when the treeview had no focused item yet. Compare it with the `list_position` of the control to find out which way the cursor went, and by how much.
