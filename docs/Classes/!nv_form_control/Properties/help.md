# help
A string defining the help (long text) of the control.

`string help;`

## Remarks:
This help will be shown when a user presses `F1` on this control focused. If you want to define what `F1` does, see `help_callback` property.
