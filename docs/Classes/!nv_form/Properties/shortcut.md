# shortcut
An instance to the `nv_shortcut_manager` object for this form. Please note that this value will be `null` until the form is created, i.e. the `create_window` method is called.

`nv_shortcut_manager@ shortcut;`
