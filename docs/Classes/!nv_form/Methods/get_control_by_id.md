# get_control_by_id
Retrieves the control by its reference ID.

`nv_form_control@get_control_by_id(const string&in id);`

## Arguments:
- `const string&in id`: The ID to search.

## Returns:
`nv_form_control@`: A handle to the control of the given ID on success, null otherwise.
